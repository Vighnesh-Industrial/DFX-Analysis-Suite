# DFX Analysis Suite - runtime requirements
#
# The analysis core (dfx_analyzers/) has NO third-party dependencies.
# You can run analyze.py on any Python 3.8+ install with nothing installed:
#
#     python analyze.py example_parts/sample_bracket.STEP
#
# The packages below are needed only for the Flask web dashboard.

Flask>=3.0.0
Flask-Cors>=4.0.0
Werkzeug>=3.0.0
